import edu.gwu.lintool.*;

public class AakashLinTool extends LinToolEmpty {

    // We'll now override the method that adds two real numbers.
    public double add (double a, double b)
    {
	double c = a + b;
	return c;
    }

}
